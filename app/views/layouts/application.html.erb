<!DOCTYPE html>
<html lang="en">
  <%= render 'layouts/head' %>
  <body>
    <%# render 'layouts/header' %>
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
    <%= debug(logged_in?) if Rails.env.development? %>
  </body>
</html>
