<!DOCTYPE html>
<html lang="en">
  <%= render 'shared/head' %>
  <body>
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
    <%= debug(logged_in?) if Rails.env.development? %>
    <%= debug(session[:user_id]) if Rails.env.development? %>
  </body>
</html>
