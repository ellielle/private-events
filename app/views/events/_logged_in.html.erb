<% event_list.each do |event| %>
  <div class="events-list">
    <ul>
      <%# TODO change event.creator_id to show creator's name %>
      <li><%= link_to "#{event.title}", event_path(event) %> Hosted by: <%= event.creator_id %></li>
    </ul>
  </div>
<% end %>
